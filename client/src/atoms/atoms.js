import { atom } from "jotai";

export const isOpenAtom = atom(false);
export const mealDataAtom = atom([]);
export const mealWholeDataAtom = atom([]);
export const modalModeAtom = atom("add");
export const selectedMealIdAtom = atom(0);
